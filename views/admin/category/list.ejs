<div class="container">
    <div class="d-flex justify-content-between mb-10 w-100">
        <h3 class="card-title align-items-start flex-column">
                                                <span class="card-label fw-bold text-gray-800">
                                                    Danh sách Danh mục
                                                </span>
        </h3>
        <a href="/admin/category/create" class="btn btn-sm fw-bold btn-primary">ADD CATEGORIES</a>
    </div>

    <div class="card-body pt-2">
        <div class="tab-content">
            <div class="tab-pane fade show active" id="kt_stats_widget_16_tab_1">
                <div class="table-responsive">
                    <table class="table table-row-dashed align-middle gs-0 gy-3 my-0">
                        <thead>
                        <tr class="fs-7 fw-bold text-gray-500 border-bottom-0">
                            <th class="p-0 pb-3 min-w-100px text-center">
                                ORD
                            </th>
                            <th class="p-0 pb-3 min-w-100px text-center pe-13">
                                NAME
                            </th>
                            <th class="p-0 pb-3 min-w-100px text-center pe-13">
                                STATUS
                            </th>
                            <th class="p-0 pb-3 w-100px text-center">ACTIONS</th>
                        </tr>
                        </thead>
                        <tbody>
                        <% if (categories && categories.length > 0) { %>
                            <% categories.forEach((category, index) => { %>
                                <tr>
                                    <td class="text-center pe-2">
                                        <span class="text-gray-600 fw-bold fs-6"><%= index + 1 %></span>
                                    </td>
                                    <td class="text-center pe-13">
                                        <span class="text-gray-600 fw-bold fs-6"><%= category.name %></span>
                                    </td>
                                    <% if (category.status === 'hoạt động') { %>
                                        <td class="text-center pe-13">
                                            <span class="text-gray-600 fw-bold fs-6">Hoạt động</span>
                                        </td>
                                    <% } else { %>
                                        <td class="text-center pe-13">
                                            <span class="text-gray-600 fw-bold fs-6">Đã xóa</span>
                                        </td>
                                    <% } %>
                                    <td class="text-center d-flex justify-content-center">
                                        <a href="/admin/category/edit/<%= category._id %>"
                                           class="btn btn-sm btn-icon btn-bg-success btn-active-color-success w-30px h-30px">
                                            <i class="fa-solid fa-pen text-light"></i>
                                        </a>
                                        <a href="#"
                                           class="btn btn-sm btn-icon btn-bg-danger btn-active-color-danger w-30px h-30px">
                                            <i class="fa-solid fa-trash text-light"></i>
                                        </a>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
